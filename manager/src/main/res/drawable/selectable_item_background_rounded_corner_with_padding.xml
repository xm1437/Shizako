<?xml version="1.0" encoding="utf-8"?>
<!-- Overrides rikkax.material drawable of the same name.
     The library version uses ?android:attr/windowBackground as a solid color,
     but in this app windowBackground is a drawable (wallpaper layer-list),
     which crashes GradientDrawable inflation. colorBackground is a real color. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt">

    <item
        android:bottom="2dp"
        android:left="4dp"
        android:right="4dp"
        android:top="2dp">

        <aapt:attr name="android:drawable">
            <ripple android:color="?android:colorControlHighlight">
                <item android:id="@android:id/mask">
                    <shape android:shape="rectangle">
                        <solid android:color="?android:attr/colorBackground" />
                        <corners android:radius="4dp" />
                    </shape>
                </item>
            </ripple>
        </aapt:attr>

    </item>

</layer-list>
