<?xml version="1.0" encoding="utf-8"?>
<!-- One-click activation page. The content scrolls under the frosted app bar
     (added by AppBarActivity), so the scroll view is padded by the action
     bar size instead of being constrained below it. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <rikka.widget.borderview.BorderNestedScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingTop="?actionBarSize"
        android:scrollbarStyle="insideOverlay"
        android:scrollbars="vertical"
        app:borderBottomVisibility="never"
        app:borderTopDrawable="@null"
        app:borderTopVisibility="whenTop"
        app:fitsSystemWindowsInsets="top|bottom">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="@dimen/rd_activity_horizontal_margin"
            android:paddingEnd="@dimen/rd_activity_horizontal_margin"
            android:paddingBottom="32dp">

            <!-- Page intro -->
            <TextView
                android:id="@+id/page_subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/activation_subtitle"
                android:textAppearance="?textAppearanceBodyMedium"
                android:textColor="?android:textColorSecondary" />

            <!-- Service status banner: green dot while the Shizaku
                 service runs, red when it is stopped. Updated live via
                 the binder listeners. -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/status_card"
                style="?attr/materialCardViewFilledStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                app:cardBackgroundColor="@color/home_card_background_color"
                app:cardCornerRadius="28dp"
                app:cardElevation="0dp"
                app:contentPadding="16dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <View
                        android:id="@+id/status_dot"
                        android:layout_width="10dp"
                        android:layout_height="10dp"
                        android:background="@drawable/shape_status_dot"
                        android:importantForAccessibility="no" />

                    <TextView
                        android:id="@+id/status_text"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:layout_weight="1"
                        android:text="@string/activation_service_not_running"
                        android:textAppearance="?textAppearanceBodyMedium" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <!-- Indeterminate progress bar shown while a command is
                 executing on the server. -->
            <com.google.android.material.progressindicator.LinearProgressIndicator
                android:id="@+id/running_indicator"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:indeterminate="true"
                android:visibility="gone"
                app:trackCornerRadius="3dp" />

            <!-- Target cards -->
            <LinearLayout
                android:id="@+id/targets_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

            <!-- Custom command section -->
            <com.google.android.material.card.MaterialCardView
                style="?attr/materialCardViewFilledStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                app:cardBackgroundColor="@color/home_card_background_color"
                app:cardCornerRadius="28dp"
                app:cardElevation="0dp"
                app:contentPadding="16dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@string/activation_custom_title"
                        android:textAppearance="?textAppearanceTitleMedium" />

                    <EditText
                        android:id="@+id/custom_command"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="12dp"
                        android:background="@null"
                        android:fontFamily="monospace"
                        android:hint="@string/activation_custom_hint"
                        android:imeOptions="actionDone"
                        android:importantForAutofill="no"
                        android:inputType="textMultiLine|textNoSuggestions"
                        android:minLines="2"
                        android:padding="12dp"
                        android:textSize="12sp" />

                    <Button
                        android:id="@+id/custom_run"
                        style="?materialButtonOutlinedStyle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="end"
                        android:layout_marginTop="8dp"
                        android:text="@string/activation_run" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <!-- Output section -->
            <com.google.android.material.card.MaterialCardView
                style="?attr/materialCardViewFilledStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                app:cardBackgroundColor="@color/home_card_background_color"
                app:cardCornerRadius="28dp"
                app:cardElevation="0dp"
                app:contentPadding="16dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:gravity="center_vertical"
                        android:orientation="horizontal">

                        <TextView
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:text="@string/activation_output"
                            android:textAppearance="?textAppearanceTitleMedium" />

                        <Button
                            android:id="@+id/output_copy"
                            style="?borderlessButtonStyle"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:minWidth="0dp"
                            android:text="@string/activation_copy"
                            android:textSize="13sp" />
                    </LinearLayout>

                    <TextView
                        android:id="@+id/output_view"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        android:fontFamily="monospace"
                        android:lineSpacingExtra="2dp"
                        android:minHeight="48dp"
                        android:text="@string/activation_output_empty"
                        android:textAppearance="?textAppearanceBodySmall"
                        android:textIsSelectable="true" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

        </LinearLayout>
    </rikka.widget.borderview.BorderNestedScrollView>
</FrameLayout>
